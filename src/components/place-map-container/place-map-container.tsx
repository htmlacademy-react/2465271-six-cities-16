import PlaceMap from '../place-map/place-map';

function PlaceMapContainer (): JSX.Element {
  return (
    <div className="cities__right-section">
      <PlaceMap/>
    </div>
  );
}

export default PlaceMapContainer;
